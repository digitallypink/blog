<body class="{{ section }}">
<nav>
    <div class="nav-wrapper">
      <a href="#" class="brand-logo">Logo</a>
      <ul id="nav-mobile" class="right hide-on-med-and-down">
        {% for sectionName in collections.sectionNames %}
          <li><a href="{{ sectionName }}">{{ sectionName }}</a></li>
        {% endfor %}
      </ul>
    </div>
</nav>
